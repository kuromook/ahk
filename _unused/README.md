# Unused top-level files

These files and folders are kept here locally as an archive instead of being deleted.

- `TT/`: HotKeyIt TT tooltip library. Active scripts now use the built-in `ToolTip` command instead. This third-party library remains gitignored and is not committed.
